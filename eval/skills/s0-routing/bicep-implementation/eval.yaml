name: bicep-implementation-trigger
description: >-
  Routing (trigger-accuracy) eval for the bicep-implementation skill. Scores whether a prompt
  should activate it, using Waza's offline `trigger` grader — keyword and USE-FOR
  phrase overlap against the skill's own description. No model is called, so this
  runs in the free S0 tier.
skill: bicep-implementation
version: "1.0"
config:
  trials_per_task: 1
  timeout_seconds: 60
  parallel: false
  executor: mock
tasks:
  - "tasks/*.yaml"
