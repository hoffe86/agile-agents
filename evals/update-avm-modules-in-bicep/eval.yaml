name: update-avm-modules-in-bicep-trigger
description: >-
  Routing (trigger-accuracy) eval for the update-avm-modules-in-bicep skill. Scores whether a prompt
  should activate it, using Waza's offline `trigger` grader — keyword and USE-FOR
  phrase overlap against the skill's own description. No model is called, so this
  runs in the free S0 tier.
skill: update-avm-modules-in-bicep
version: "1.0"
config:
  trials_per_task: 1
  timeout_seconds: 60
  parallel: false
  executor: mock
tasks:
  - "tasks/*.yaml"
