id: git-commit-negative-1
name: Should NOT route to git-commit
description: >-
    Unrelated authoring work; commit tooling should stay out.
tags:
  - negative-trigger
inputs:
  prompt: >-
    Add pytest cases for this Python module, including the error paths.
expected:
  should_trigger: false
graders:
  - type: trigger
    name: stays-out-of-git-commit
    config:
      skill_path: plugins/agile-agents-core/skills/git-commit/SKILL.md
      mode: negative
      threshold: 0.6
